{
  "extends": [
    "@commitlint/config-conventional"
  ],
  "formatter": "@commitlint/format",
  "rules": {
    "type-enum": [
      2,
      "always",
      [
        "release"
      ]
    ],
    "header-max-length": [
      0,
      "always",
      256
    ]
  }
}