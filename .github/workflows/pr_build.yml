name: PR build
on: [pull_request]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Setup
        uses: actions/setup-node@v3
        with:
          node-version: 16.x
      - name: Install
        run: npm i
      - name: Test, build
        run: npm run build
