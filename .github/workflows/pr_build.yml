name: PR build
on: [pull_request]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Install
        uses: actions/setup-node@v3
        with:
          node-version: 16.x
      - name: Test, build
        run: npm run build
